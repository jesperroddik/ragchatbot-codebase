# Copy this file to .env and adjust if needed.
# This system uses a local Ollama model via its OpenAI-compatible API.
# No API key is required for local Ollama; these have sensible defaults.
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.1
